services:
  - type: web
    name: green-room
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: K2THINK_API_KEY
        sync: false
      - key: K2THINK_BASE_URL
        sync: false
      - key: PORT
        generateValue: true
